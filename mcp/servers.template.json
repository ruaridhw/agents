{
  "//": [
    "MCP server definitions with ${ENV_VAR} placeholders, resolved from .env",
    "at load time by runner/config.py. Each job selects only the servers it",
    "needs (jobs/<name>/job.py), so unused connectors never block a run.",
    "",
    "Remote (url) servers authenticate via OAuth: run `scripts/render-mcp-json.py`",
    "then `claude` in this repo and use /mcp to complete each OAuth once — the",
    "Claude Code binary caches the tokens and headless SDK runs reuse them.",
    "",
    "Google's remote MCP endpoints (calendarmcp/gmailmcp.googleapis.com) only",
    "accept claude.ai's pre-registered OAuth client — no dynamic client",
    "registration — so gcal/gmail run as stdio servers against your own Google",
    "Cloud OAuth client. Both share the same OAuth client JSON; each stores its",
    "granted token locally after a one-time `npx <pkg> auth` browser flow."
  ],
  "servers": {
    "notion": {
      "type": "http",
      "url": "https://mcp.notion.com/mcp"
    },
    "granola": {
      "type": "http",
      "url": "https://mcp.granola.ai/mcp"
    },
    "linear": {
      "type": "http",
      "url": "https://mcp.linear.app/mcp"
    },
    "slack": {
      "type": "http",
      "url": "https://mcp.slack.com/mcp"
    },
    "gcal": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@cocal/google-calendar-mcp"],
      "env": {
        "GOOGLE_OAUTH_CREDENTIALS": "${GOOGLE_OAUTH_CREDENTIALS}"
      }
    },
    "gmail": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@gongrzhe/server-gmail-autoauth-mcp"]
    }
  }
}
