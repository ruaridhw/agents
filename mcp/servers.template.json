{
  "//": [
    "MCP server definitions with ${ENV_VAR} placeholders, resolved from .env",
    "at load time by runner/config.py. Each job selects only the servers it",
    "needs (jobs/<name>/job.py), so unused connectors never block a run.",
    "",
    "Remote (url) servers authenticate via OAuth: run `scripts/render-mcp-json.py`",
    "then `claude` in this repo and use /mcp to complete each OAuth once — the",
    "Claude Code binary caches the tokens and headless SDK runs reuse them.",
    "",
    "Google's remote MCP endpoints speak standard bearer auth but their OAuth",
    "server (accounts.google.com) has no dynamic client registration, so",
    "`claude mcp login` can't drive them. Instead scripts/google-oauth.py mints",
    "tokens from your own Google OAuth desktop client (one-time `authorize`,",
    "silent refresh per run); ${GOOGLE_MCP_ACCESS_TOKEN} is computed at job",
    "load by runner/config.py, never stored in .env."
  ],
  "servers": {
    "notion": {
      "type": "http",
      "url": "https://mcp.notion.com/mcp"
    },
    "granola": {
      "type": "http",
      "url": "https://mcp.granola.ai/mcp"
    },
    "linear": {
      "type": "http",
      "url": "https://mcp.linear.app/mcp"
    },
    "slack": {
      "type": "http",
      "url": "https://mcp.slack.com/mcp"
    },
    "gcal": {
      "type": "http",
      "url": "https://calendarmcp.googleapis.com/mcp/v1",
      "headers": {
        "Authorization": "Bearer ${GOOGLE_MCP_ACCESS_TOKEN}"
      }
    },
    "gmail": {
      "type": "http",
      "url": "https://gmailmcp.googleapis.com/mcp/v1",
      "headers": {
        "Authorization": "Bearer ${GOOGLE_MCP_ACCESS_TOKEN}"
      }
    }
  }
}
